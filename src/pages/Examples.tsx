import { MultipleCustomHooks } from "@/content";
import { Container } from "@mui/material";

export const Examples: React.FC = () => {
  return (
    <Container>
      <MultipleCustomHooks />
    </Container>
  );
};

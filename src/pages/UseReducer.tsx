import { Container } from "@mui/material";
import "@/content/08-useReducer/introReducer";
import { TodoApp } from "@/content";

export const UseReducer: React.FC = () => {
  return (
    <Container>
      <TodoApp />
    </Container>
  );
};
